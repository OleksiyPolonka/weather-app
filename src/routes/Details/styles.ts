import { styled } from '@mui/material/styles';
import { Grid } from '@mui/material';

export const ForecastGridContainer = styled(Grid)(() => ({
  overflow: 'auto',
  margin: '16px',
  height: '55vh',
}));
